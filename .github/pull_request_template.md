## What and why

<!-- What this changes, and the problem it solves. Link the issue: "Fixes #123". -->

## How it was tested

<!-- The tests you added or ran, and what you checked by hand. -->

## Checklist

- [ ] `make lint` and `make test` pass
- [ ] `npm --prefix web run build` passes (if the UI changed; new strings are translated in `web/src/locales/fr.ts`)
- [ ] An entry under `## [Unreleased]` in `CHANGELOG.md`, if users will notice the change
- [ ] The docs are updated (README, `docs/`, chart `values.yaml`), if behaviour or configuration changed
