# Binaries
/bin/
/dist/
quetzal-apiserver
quetzal-controller
quetzal-sftp
# bare `go build ./cmd/<x>` outputs at the repo root
/sftp
/configrender
/activator
*.exe

# Local data / DB
*.db
*.db-journal
*.sqlite
data/

# Go
/vendor/

# Node / frontend
web/node_modules/
# Built UI is embedded at build time but not committed; keep the dir via .gitkeep
# so `go build` (which embeds web/dist) works without a frontend build.
web/dist/*
!web/dist/.gitkeep

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Secrets / local config
*.local.yaml
kubeconfig
